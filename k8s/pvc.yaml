apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: liberu-maintenance-storage
  namespace: liberu-maintenance
  labels:
    app.kubernetes.io/name: liberu-maintenance
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: standard
  resources:
    requests:
      storage: 10Gi
