apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maintenance-storage
  namespace: liberu-maintenance
  labels:
    app: liberu-maintenance
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
